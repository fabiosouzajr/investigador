# Investigador

## Setup

1. Activate the virtual environment:
   ```bash
   source .venv/bin/activate
   ```

2. Install dependencies (when added):
   ```bash
   pip install -e .
   ```

## Environment Variables

Copy `.env` and add your environment variables as needed.


